import { AlarmPointsFilterPipe } from './alarm-points-filter.pipe';

describe('AlarmPointsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlarmPointsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
